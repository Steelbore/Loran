+++
name = "rg"
category = "text-search"
summary = "Ripgrep — recursively search directories for a regex pattern."
replaces = ["grep"]
safe_alias_for = ["grep"]
aliases = ["ripgrep"]
tags = ["search"]
+++

Test fixture for the Loran index ingester. Not user-facing content.
