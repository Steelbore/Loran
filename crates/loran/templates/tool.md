+++
name           = "{{name}}"
category       = "{{category}}"
summary        = "{{summary}}"
replaces       = [{{replaces}}]
safe_alias_for = [{{safe_alias_for}}]
pairs_with     = []
# official    = ""
# tldr_page   = "{{name}}"
# written_in  = ""
# since       = ""
tags           = []
+++

# {{name}}

Spacecraft Software notes:

- Why we ship {{name}} as the canonical option for this category.
- The legacy tool(s) it replaces and what's lost by aliasing.
- A representative command users will actually paste.
- Anything `--help` or tldr-pages alone leaves out.
