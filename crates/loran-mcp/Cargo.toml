# SPDX-License-Identifier: GPL-3.0-or-later
# SPDX-FileCopyrightText: 2026 Mohamed Hammad

[package]
name = "loran-mcp"
description = "Loran — read-only MCP server surface (list / show / find / search / categories)."
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords = ["man", "help", "mcp", "agent", "jsonrpc"]
categories = ["api-bindings", "development-tools"]

[dependencies]
loran-core = { path = "../loran-core", version = "0.4.1" }
loran-index = { path = "../loran-index", version = "0.4.1" }
loran-pages = { path = "../loran-pages", version = "0.4.1" }
schemars = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
